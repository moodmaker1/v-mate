# v-mate 설치 및 사용 가이드 💡

## 📦 설치 방법

### 1. Chrome 브라우저에 확장 프로그램 로드

1. Chrome 브라우저에서 `chrome://extensions/` 페이지를 엽니다
2. 우측 상단의 **"개발자 모드"**를 활성화합니다
3. **"압축해제된 확장 프로그램을 로드합니다"** 버튼을 클릭합니다
4. 이 프로젝트 폴더(`v-mate`)를 선택합니다

### 2. 아이콘 이미지 준비

현재 `icons/` 폴더에 아이콘 이미지가 필요합니다:
- `icon16.png` (16x16 픽셀)
- `icon48.png` (48x48 픽셀)
- `icon128.png` (128x128 픽셀)

**임시 아이콘 생성 방법:**
- [Favicon Generator](https://www.favicon-generator.org/)를 사용하여 💡 이모지 이미지를 생성
- 또는 간단한 로고 이미지를 다운로드하여 사용

### 3. OpenAI API 키 설정

1. [OpenAI Platform](https://platform.openai.com/api-keys)에서 API 키를 발급받습니다
2. Chrome 브라우저 우측 상단의 v-mate 아이콘을 클릭합니다
3. API 키를 입력하고 **"저장"** 버튼을 클릭합니다

## 🎯 사용 방법

### 기본 사용법

1. **텍스트 선택**: 웹페이지에서 궁금한 단어나 문장을 드래그합니다
2. **아이콘 클릭**: 선택한 텍스트 옆에 나타나는 💡 아이콘을 클릭합니다
3. **설명 확인**: AI가 생성한 설명을 팝업에서 확인합니다

### 주요 기능

- ✅ **3글자 이상** 선택 시에만 작동
- ✅ **input/textarea 제외**: 입력 필드에서는 작동하지 않음
- ✅ **페이지 맥락 고려**: 현재 페이지의 제목과 URL을 함께 전송하여 더 정확한 설명 제공
- ✅ **ESC 키로 닫기**: 팝업이나 아이콘을 ESC 키로 빠르게 닫을 수 있음
- ✅ **외부 클릭으로 닫기**: 팝업 외부를 클릭하면 자동으로 닫힘

## 🔧 개발자 모드 테스트

### 콘솔 로그 확인

확장 프로그램이 정상 작동하는지 확인하려면:

1. 웹페이지에서 F12를 눌러 개발자 도구를 엽니다
2. **Console** 탭을 선택합니다
3. 텍스트를 드래그하면 다음과 같은 로그가 출력됩니다:

```
v-mate content script loaded
=== v-mate 텍스트 선택 감지 ===
선택된 텍스트: [선택한 텍스트]
페이지 제목: [페이지 제목]
URL: [페이지 URL]
==============================
선택 영역 위치: {...}
```

### Background Script 로그 확인

1. `chrome://extensions/` 페이지로 이동합니다
2. v-mate 확장 프로그램에서 **"service worker"** 링크를 클릭합니다
3. API 호출 로그를 확인할 수 있습니다:

```
v-mate background service worker loaded
Background received message: {...}
설명 요청 받음: {...}
GPT API 호출 중...
GPT 응답: [AI 설명]
```

## 🐛 문제 해결

### API 키 오류

**증상**: "API 키가 설정되지 않았습니다" 오류 메시지
**해결**: 확장 프로그램 팝업에서 API 키를 다시 설정해주세요

### 아이콘이 표시되지 않음

**증상**: 텍스트를 선택해도 💡 아이콘이 나타나지 않음
**해결**:
1. 3글자 이상 선택했는지 확인
2. input/textarea가 아닌 일반 텍스트인지 확인
3. 콘솔에서 오류 메시지 확인

### API 호출 실패

**증상**: "API 오류" 메시지가 표시됨
**해결**:
1. API 키가 올바른지 확인
2. OpenAI 계정에 크레딧이 있는지 확인
3. 인터넷 연결 상태 확인

### 확장 프로그램이 로드되지 않음

**증상**: Chrome에서 확장 프로그램 로드 실패
**해결**:
1. `manifest.json` 파일이 프로젝트 루트에 있는지 확인
2. 아이콘 파일이 `icons/` 폴더에 있는지 확인 (임시로 아무 이미지라도 배치)
3. Chrome 확장 프로그램 페이지에서 오류 메시지 확인

## 💰 비용 정보

- **모델**: GPT-4o-mini
- **예상 비용**: 매우 저렴 (요청당 약 $0.0001~0.0002)
- **max_tokens**: 200으로 제한되어 있어 비용 효율적

## 🔒 개인정보 보호

- API 키는 브라우저 로컬 스토리지에만 저장됩니다
- 선택한 텍스트와 페이지 정보만 OpenAI API로 전송됩니다
- 다른 서버나 제3자와 데이터를 공유하지 않습니다

## 📝 개발 정보

- **Manifest Version**: 3
- **최소 Chrome 버전**: 88+
- **사용 언어**: Vanilla JavaScript
- **API**: OpenAI GPT-4o-mini

## 🙋‍♂️ 지원

문제가 발생하거나 개선 제안이 있으시면 이슈를 등록해주세요!

