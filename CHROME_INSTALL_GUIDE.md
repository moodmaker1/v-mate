# 🚀 Chrome 확장 프로그램 설치 가이드

## 1단계: Chrome 확장 프로그램 페이지 열기

1. Chrome 브라우저를 엽니다
2. 주소창에 다음을 입력하고 Enter:
   ```
   chrome://extensions/
   ```
   또는 메뉴에서:
   - 우측 상단 점 3개 (⋮) 클릭
   - **확장 프로그램** → **확장 프로그램 관리**

---

## 2단계: 개발자 모드 활성화

페이지 우측 상단에 **"개발자 모드"** 토글 스위치를 찾아서 **켜기(ON)**로 설정합니다.

```
┌─────────────────────────────────────┐
│  확장 프로그램              개발자 모드 ○→● │
└─────────────────────────────────────┘
```

개발자 모드를 켜면 3개의 버튼이 나타납니다:
- 압축해제된 확장 프로그램을 로드합니다
- 압축된 확장 프로그램을 로드합니다
- 압축된 확장 프로그램을 업데이트합니다

---

## 3단계: 확장 프로그램 로드

1. **"압축해제된 확장 프로그램을 로드합니다"** 버튼을 클릭합니다

2. 파일 탐색기가 열리면 **v-mate 프로젝트 폴더**를 선택합니다
   ```
   E:\projects\v-mate
   ```

3. **"폴더 선택"** 버튼을 클릭합니다

---

## 4단계: 설치 확인

확장 프로그램이 추가되면 다음과 같이 표시됩니다:

```
┌─────────────────────────────────────────┐
│ 💡 v-mate                         1.0.0  │
│ 대학생을 위한 AI 학습 도우미              │
│                                          │
│ ID: abcdefghijklmnopqrstuvwxyz          │
│ ✓ 사이트 데이터 읽기 및 변경             │
│                                          │
│ 🔄 새로고침   🗑️ 삭제   세부정보        │
└─────────────────────────────────────────┘
```

**확인 포인트:**
- ✅ v-mate 이름이 표시됨
- ✅ "오류" 버튼이 없음 (오류가 있으면 빨간색으로 표시됨)
- ✅ 확장 프로그램이 활성화되어 있음 (토글이 파란색)

---

## 5단계: 확장 프로그램 고정 (선택사항)

브라우저 툴바에 아이콘을 고정하려면:

1. Chrome 우측 상단의 **퍼즐 조각 아이콘** 클릭
2. v-mate 옆의 **핀 아이콘** 클릭
3. 이제 툴바에 v-mate 아이콘이 항상 표시됩니다

---

## 6단계: 테스트!

### 기본 테스트

1. **아무 웹페이지나 열기** (예: 위키백과, 블로그 등)
   ```
   예: https://ko.wikipedia.org/wiki/인공지능
   ```

2. **텍스트를 드래그**해서 선택 (3글자 이상)
   ```
   예: "머신러닝" 또는 "데이터베이스" 등
   ```

3. **💡 아이콘이 나타나는지 확인**
   - 선택한 텍스트 오른쪽 상단에 보라색 아이콘이 표시됩니다

4. **💡 아이콘을 클릭**

5. **팝업이 표시되고 로딩이 시작됨**
   - "AI가 설명을 생성하고 있습니다..." 메시지와 스피너 표시

6. **GPT 설명이 표시되면 성공!** 🎉

---

## 문제 해결

### ❌ 확장 프로그램 로드 실패

**오류 메시지: "manifest.json을 찾을 수 없습니다"**

**해결:**
- v-mate 폴더를 정확히 선택했는지 확인
- manifest.json 파일이 폴더 안에 있는지 확인

### ❌ 아이콘이 표시되지 않음

**원인 1: 확장 프로그램이 활성화되지 않음**
- `chrome://extensions/`에서 v-mate 토글이 켜져 있는지 확인

**원인 2: 페이지 새로고침 필요**
- 확장 프로그램을 설치한 후 웹페이지를 새로고침(F5)

**원인 3: 텍스트 선택이 짧음**
- 3글자 이상 선택해야 함

### ❌ "API 키가 설정되지 않았습니다" 오류

**원인: config.js의 API 키가 잘못됨**

**해결:**
1. config.js 파일을 다시 확인
2. API 키가 정확히 입력되었는지 확인
3. `chrome://extensions/`에서 v-mate 새로고침 버튼 클릭

### ❌ API 호출 실패

**가능한 원인:**
- API 키가 만료되었거나 잘못됨
- OpenAI 크레딧이 소진됨
- 인터넷 연결 문제

**해결:**
1. OpenAI 플랫폼에서 API 키 상태 확인: https://platform.openai.com/account/api-keys
2. 사용량 확인: https://platform.openai.com/usage
3. 인터넷 연결 확인

---

## 콘솔 로그 확인 (개발자용)

### Content Script 로그 확인

1. 웹페이지에서 **F12** 눌러서 개발자 도구 열기
2. **Console** 탭 선택
3. 텍스트 드래그하면 다음과 같은 로그가 표시됨:

```
v-mate content script loaded
=== v-mate 텍스트 선택 감지 ===
선택된 텍스트: 머신러닝
페이지 제목: 인공지능 - 위키백과
URL: https://ko.wikipedia.org/...
```

### Background Script 로그 확인

1. `chrome://extensions/` 페이지 열기
2. v-mate 확장 프로그램에서 **"service worker"** 링크 클릭
3. 새 창이 열리면 Console 탭에서 로그 확인:

```
v-mate background service worker loaded
API 키 소스: config.js
Background received message: ...
GPT API 호출 중...
GPT 응답: [설명 내용]
```

---

## 확장 프로그램 업데이트

코드를 수정한 후:

1. `chrome://extensions/` 페이지 열기
2. v-mate 확장 프로그램의 **🔄 새로고침** 버튼 클릭
3. 또는 확장 프로그램을 껐다가 다시 켜기

---

## 확장 프로그램 제거

필요없을 때:

1. `chrome://extensions/` 페이지 열기
2. v-mate 확장 프로그램의 **🗑️ 삭제** 버튼 클릭
3. 확인 팝업에서 **"삭제"** 클릭

---

## 빠른 체크리스트 ✅

```
□ Chrome 열기
□ chrome://extensions/ 접속
□ 개발자 모드 ON
□ "압축해제된 확장 프로그램을 로드합니다" 클릭
□ v-mate 폴더 선택
□ 웹페이지 열기
□ 텍스트 드래그 (3글자 이상)
□ 💡 아이콘 클릭
□ 설명 확인!
```

---

## 다음 단계

- 다양한 웹페이지에서 테스트해보세요!
- 전문 용어, 영어 단어, 어려운 개념 등을 선택해서 설명을 받아보세요
- 문제가 있으면 콘솔 로그를 확인하세요

즐거운 학습 되세요! 💡✨

